# STATUS-2026-09-20 — สถานะ repository หลังงานวันแรก

**PRP — Private Runtime Platform | `main` = `ff4e0fd` | 2026-09-20 | เขียนตอนปิด session เพื่อให้ session ถัดไปเริ่มได้โดยไม่ต้องอ่านประวัติสนทนา**

เอกสารนี้เป็น governance working memory (`.brain/`) ไม่ใช่ canonical doc ความจริงทางเทคนิคทุกข้ออยู่ใน `docs/` และ `docs/CHANGELOG-PRP.md` "Unreleased — v0.4.0-draft" ซึ่งมี entry ต่อ PR ครบ ถ้าสองที่ขัดกัน ให้เชื่อ `docs/`

## 1. จากอะไรไปถึงอะไร

| | เช้า (commit `745c401`) | เย็น (`ff4e0fd`, 40 commits, 13 PR) |
|---|---|---|
| เนื้อหา | package เอกสาร v0.3.0 อย่างเดียว | monorepo: living docs + contracts + Python skeleton 2 project + tooling + CI + branch protection + WP24 เริ่มรันจริง |
| code | ไม่มี | `apps/control-api` (45 ไฟล์ src, 50 tests), `workers/voice` (12 ไฟล์, 22 tests) fail closed ทุกทาง |
| contracts | client YAML เดียว | client / worker / management YAML canonical + JSON derived + generated Pydantic models (ADR-PRP-013) |
| การรับรอง | ทุก AT NOT_RUN | **ยังคง NOT_RUN ทั้ง 92** WP 25 ตัว NOT_STARTED framework OPEN ตาม design |

## 2. PR ที่ merge วันนี้ (ทั้งหมดเป็น merge commit ไม่ squash)

| PR | merge | เรื่อง |
|---|---|---|
| #1 | `58d40ed` | M1 monorepo layout, M2 contracts, M3 skeletons, trace tooling, WP24 fit-gap prep, ADR-PRP-013 items 1–5 (8 commits) |
| #3 | `ab6f6ae` | workflows ทั้ง 5 รันทุก PR เพราะเป็น required checks ของ `main` |
| #2 | `bdfe860` | ลบ section ARTEMIS ที่ไม่เกี่ยวออกจาก AGENTS.md (session แยก) |
| #4 | `3f0cdb1` | `docs/WP24-EXPERIMENT-PROCEDURE.md` + `registry/wp24-run-record-template.json` (owner approved) |
| #5 | `30a7d39` | LiteLLM เฉพาะ EV04 + EV03 ใน candidate B time box 1 วัน; BYOM licensing |
| #6 | `ecf4a7b` | run record `WP24-2026-09-20-run1`: EV01 รันจริง |
| #7 | `43cb02c` | แก้ precondition EV01 ที่เขียนเกินข้อกำหนด (control co-locate บน GPU host ได้) + RCA |
| #8 | `189ee24` | reviewer = owner (บันทึก no independent reviewer), time box 3 วันทำงาน/candidate, C นอก scope, EV07 BLOCKED จน WP10 |
| #9 | `bb0a4d5` | `tools/wp24/` เครื่องมือ operator สำหรับ EV02 (stdlib only) |
| #11 | `d68bed5` | `shared_revision` = `typhoon-ai/typhoon2.5-qwen3-4b@ce0a741`; B = vLLM ยืนยัน |
| #10 | `36fca1b` | `tools/wp24/litellm_subspike/` kit สำหรับ EV04 + EV03 |
| #12 | `b186c16` | inventory ของเครื่อง control ลง record |
| #13 | `ff4e0fd` | `host_inventory.py` อ่าน `CUDA UMD Version:` และวัด NTP offset ตรง (`--ntp-check`) |

PR #8, #9, #10 ผลิตโดย Sonnet subagent ใน worktree แยก และผ่านการตรวจใน session (tests / ruff / stdlib-only / secret scan) ก่อน merge

## 3. คำตัดสินของ owner ที่บันทึกแล้ว (ไม่ต้องถามซ้ำ)

- ADR-PRP-012 (monorepo) และ ADR-PRP-013 (generated contract models) ACCEPTED ทุก action item ปิดแล้ว
- Python 3.12; FastAPI + Pydantic เป็น candidate; adapters / engines / `deploy/` เลื่อนไป M4
- `main` มี branch protection: 5 required checks, `strict: false`, ไม่บังคับ review, admin bypass ได้ตาม Hotfix rule, ห้าม force-push / ลบ; `allow_auto_merge` ยังปิด
- WP24: candidate A = Xinference, **B = vLLM ตามเดิม** (Ollama / GGUF ไม่ใช้กับ PRP; Ollama เป็น dev target ของ chatbot client ได้), C นอก scope, speech นอก scope (EV07 BLOCKED จน WP10), LiteLLM เฉพาะ EV04 + EV03 ใน B ไม่เกิน 1 วันทำงาน routing ของมันไม่แทน PRP Router
- `shared_revision` = `typhoon-ai/typhoon2.5-qwen3-4b` revision `ce0a7416fe82d4404b2b4a253ce3ac095ab1252c`, Apache-2.0, safetensors bf16 7.49 GiB, `Qwen3ForCausalLM`, artifact เดียวกันทั้ง A และ B ไม่ quantize; `--max-model-len` เลือกตอน EV02
- BYOM: owner นำ model มาเอง แต่ license receipt ตาม SEC-007 ยังต้องยื่นก่อน activation
- operator = reviewer = owner (บันทึกใน `decision_receipt.tests_and_blocked_items`); time box 3 วันทำงานต่อ candidate
- Lalin-AI [SRC-07] เป็น reuse candidate ระดับ engine ของ speech worker ตาม FR-043 / WP10 ไม่ใช่ผู้ส่งมอบ worker
- control plane อยู่บนเครื่องเดียวกับ GPU runtime ได้ (SRS topology, ARCH §3) ข้อกำหนดคือ process / dependency isolation (NFR-021/022) ไม่ใช่แยกเครื่อง

## 4. สถานะ WP24 (record `docs/evidence/wp24/WP24-2026-09-20-run1.json`)

| EV | PRP | A | B | C |
|---|---|---|---|---|
| EV01 Clean control install | PASS (NFR-019/021/022) | PASS (`xinference-client` 3.4.0 ไม่ดึง ML) | PASS (HTTP only) | BLOCKED (นอก scope) |
| EV02–EV06, EV08 | — | NOT_RUN | NOT_RUN | BLOCKED |
| EV07 Mixed chat/speech | — | BLOCKED จน WP10 | BLOCKED จน WP10 | BLOCKED |

- DEV-01 (เครื่องมี GPU driver) ปิดแล้วในฐานะ procedure defect ไม่ใช่ deviation จาก requirement
- inventory ของเครื่อง control อยู่ใน record: RTX 5060 Ti 16311 MiB, driver 616.92, CUDA UMD 13.4, Windows 10.0.26200, Python 3.12.10, Docker 29.8.0, disk ว่าง ~217 GiB บน `F:\`, NTP offset −0.63 s
- ยังไม่มี fit-gap copy (`registry/reuse-fit-gap.<record_id>.json`) จนกว่า candidate แรกจะครบ EV01–EV08

## 5. สิ่งที่ค้าง (เรียงตามลำดับที่ต้องทำ)

1. **ทางเข้า host A (12 GB) และ B (16 GB)** — ตัวขวางเดียวของ EV02 ขั้นแรกบนแต่ละเครื่อง: `python tools/wp24/host_inventory.py --host-id <A|B> --weights-path <path> --ntp-check --out wp24-out` แล้วทำตาม `tools/wp24/EV02-CHECKLIST.md`
2. license receipt ตาม SEC-007 / SECURITY-DATA §8 สำหรับ `shared_revision` ก่อน activation
3. เลือก `--max-model-len` ตอน launch และบันทึกต่อ candidate
4. หลัง EV ครบต่อ candidate: สร้าง fit-gap copy, `decision_receipt` (STACK §8) → เปิด WP03
5. งานเล็กที่รู้แล้ว: เพิ่ม `.claude/worktrees/` ใน `.gitignore`; multipart binding ของ `transcribeAudio` / `uploadArtifact` ที่ M4; `Definition-of-Done` / `Risk-Assessment` / `Verification-Standards` ยังอ้าง Rust/Tauri; HTML builder header ยัง "v0.3.0"; ทุกอย่างอยู่ใน Unreleased ยังไม่ออก release snapshot v0.4.0

## 6. วิธีเริ่ม session ถัดไป

```sh
git checkout main && git pull --ff-only
python tools/docs/validate_docs.py          # ต้อง errors: []
```

จากนั้นอ่าน `docs/CHANGELOG-PRP.md` Unreleased (entry ท้ายสุด = งานล่าสุด), `docs/WP24-EXPERIMENT-PROCEDURE.md` และ record ใน `docs/evidence/wp24/` งานทุกชิ้นเข้า `main` ผ่าน PR เท่านั้น (required checks 5 ตัว) และ commit / push / PR / merge ทำเมื่อ owner สั่ง
